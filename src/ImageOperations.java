public interface ImageOperations {

  /**
   * 
   */
  void apply(Image img);
}

  // TODO: do filtering (blur, sharpen) and color transformations (monochrome, sepia)
  // need to be categorized differently? aka, do they have a lot of shared code?

